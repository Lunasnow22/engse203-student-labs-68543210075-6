# AI Usage Log — สอบกลางภาค

ชื่อ-รหัส: นิรันดร์รักษ์ อนุสนธิ์ 68543210075-6

บันทึกทุกครั้งที่ใช้ AI ระหว่างสอบ

| เวลา | งาน  | ถาม AI ว่าอะไร | ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|------|-----|---------------|---|---|
|10:30 |  B1 | จะเเก้ต้อส่วนนี้ไปที่ไฟล์ใหน |Part-B/src/components/SummaryPanel.jsx: เป็นไฟล์คอมโพเนนต์หลักสำหรับแก้ไขหน้าตา UI, ข้อความกำกับ (เช่น "ทั้งหมด", "รอดำเนินการ", "กำลังดำเนินการ", "เสร็จสิ้น") และโครงสร้างของกล่องสถิติทั้ง 4 กล่อง | ด้วยการลบ ! ออก/: requests.filter((request) => request.status !== statusFilter); ทำให้ งานตรงกับ ที่เเสดงในDashboard |
|10:45 |  B1 | เเก้ไฟล์ใหน | RequestList.jsx | เเค่เพิ่มอันนี้ก็หาย เเดงเเล้ว key={request.id} |
|10:55 |  B1 | บั๊ก 4 — เปิด #/requests/REQ-001 แล้วแก้URL เป็น REQ-002 — ข้อมูลบนหน้าจอไม่เปลี่ยน | เราต้องไปแก้ที่ไฟล์ Part-B/src/pages/RequestDetailPage.jsx ครับ | }, [reloadKey]); เเค่เพิ่ม requestId ด้วยการลบดู|
|11:20 |  B1 |กดปุ่ม “ลบ” ที่การ์ด — รายการยังอยู่บนจอ ต้อง รีเฟรชถึงหาย |จุดที่ต้องแก้คือฟังก์ชัน handleDelete ในไฟล์ DashboardPage.jsx ครับ | เเค่เปลียนตัวนี้setRequests(requests);ให้เป็น setRequests(nextRequests);เเล้วลองลบดู|
|11:25 |  B1 | กดปุ่ม “Reset Demo Data” แล้ว หน้าพัง/ว่างเปล่า| สาเหตุอยู่ในไฟล์ Part-B/src/pages/DashboardPage.jsx ตรงฟังก์ชัน handleReset ครับ ฟังก์ชัน resetRequests() เป็นฟังก์ชันแบบ Asynchronous ต้องรอให้มันทำงานเสร็จก่อนถึงจะได้ข้อมูลกลับมาเป็น Array แต่โค้ดเดิมลืมใส่ await|ไปเเก้ตรง setRequests(resetRequests()); เป็น setRequests(await resetRequests()); |
|11:30 |  B2 | ช่องค้นหาแสดงและรับ input|ให้หาบรรทัดที่มีคอมเมนต์ // TODO B2: เพิ่ม state สำหรับข้อความค้นหา ที่นี่ |เเล้วเพิ่ม const [searchQuery, setSearchQuery] = useState(''); เเละ<input type="search" placeholder="ค้นหาจากผู้แจ้งหรือรายละเอียด" value={searchQuery}onChange={(e) => setSearchQuery(e.target.value)}      style={{width: '100%', padding: '0.5rem', marginBottom: '1rem' }}/> กด F5 เเล้วจะมีช่องค้นหาขึ้นมา|
|11:40 |  B2 | กรองตามผู้แจ้ง/รายละเอียด (3 คะแนน)กรองรายการที่ requesterName หรือ details มีข้อความที่พิมพ์(ไม่สนตัวพิมพ์เล็กใหญ่)| เราจะต้องแก้ไขตัวแปร filteredRequests ในไฟล์ DashboardPage.jsx เพื่อให้มันนำเอาค่า searchQuery ที่ผู้ใช้พิมพ์ มากรองเทียบกับ requesterName หรือ details แบบไม่สนตัวพิมพ์เล็ก-ใหญ่ (ใช้ .toLowerCase()) ครับ|1. เช็คเงื่อนไขสถานะ 2. เช็คเงื่อนไขค้นหา (เพิ่ม request.id เข้าไปเช็คด้วย)เเละเพิ่มrequest.id.toLowerCase().includes(query) |
|11:45 |  B2 | ค้นหาต้องกรองซ้อนกับตัวกรองสถานะเดิม เช่น กรอง completed + พิมพ์ห้อง|ตอนนี้เงื่อนไขการกรองซ้อนกัน (matchesStatus && matchesSearch) ที่เราใส่ไว้ในฟังก์ชัน filteredRequests รองรับการทำงานร่วมกับตัวกรองสถานะเรียบร้อยแล้วครับ!ระบบจะทำการกรองแบบ AND (ต้องตรงทั้งเงื่อนไขสถานะและคำค้นหาพร้อมกัน) โดยอัตโนมัติ|ลองค้นหา ว่า ตรงตามตัวกรองไหม|
|11:50 |  B2 |ถ้าค้นแล้วไม่เจอ แสดงข้อความ “ไม่พบคำร้องที่ตรงกับการค้นหา” · แผงสรุปยังนับจากข้อมูลทั้งหมด ไม่เปลี่ยนตามการค้นหา |  มาเช็คโค้ดในไฟล์ DashboardPage.jsx ของเรากันครับ:    | เช็คตัวแปรแผงสรุป (summary) ว่าใช้ requests.length ทั้งหมดหรือยัง (ของเดิมที่เราทำไว้ถูกต้องแล้ว เพราะใช้ requests.filter จาก requests ตัวเต็มโดยตรง): เช็คข้อความตอนไม่พบข้อมูล ในไฟล์ RequestList.jsx ให้แน่ใจว่าเป็นข้อความเป๊ะๆ ตามที่โจทย์ต้องการ: เเล้วทำสอบด้วยการพิม ZZZ ข้อมูลจะหาไปหมดเลย เเต่ ด้านบนจะยังสรุปเหมือนเดิน ไม่มีการเปลียนตาค้นหา     |
|12:00 |  B3 |เพิ่มปุ่ม “ ” ทำ เสร็จ ที่การ์ด |เพิ่มปุ่ม "ทำเสร็จ" ในคอมโพเนนต์การ์ด (RequestCard.jsx) |<button type="button" className="button secondary small"onClick={() => onMarkDone(request.id)}>ทำเสร็จ</button> ให้เพิ่ม code นี้เข้าไปเเล้วปุ่ม เสร็จงานจะขึ้นมา |
|12:11 |  B3 |รอด refresh (persist จริง)กด F5 รีเฟรชหน้า — สถานะที่เพิ่งเปลี่ยนต้อง ยังอยู่(เพราะเขียนลง localStorage ผ่านserviceแล้ว)|รอด refresh (persist จริง)กด F5 รีเฟรชหน้า — สถานะที่เพิ่งเปลี่ยนต้อง ยังอยู่(เพราะเขียนลง localStorage ผ่านserviceแล้ว) | กดปุ่ม F12ดูที่เมนูด้านซ้าย ขยายหัวข้อ Local Storage แล้วคลิกเลือก URL ของเว็บไซต์เรา (http://localhost:5173)ตรวจดูคีย์ชื่อ engse203-campus-requests-v1 จะต้องเห็นข้อมูล JSON ที่ถูกอัปเดตสถานะเป็น completed บันทึกอยู่ข้างในนั้นจริง ๆ|
|12:30| B4|สร้าง component ใหม่ src/components/PriorityBadge.jsx แล้วนำไปใช้แทนการแสดงpriority ดิบใน RequestCard|สร้างไฟล์คอมโพเนนต์ใหม่ PriorityBadge.jsx|function PriorityBadge({ priority }) {return (<span className={`priority-badge ${priority}`}>{priority}</span>);}export default PriorityBadge; เเละเพิ่ม import PriorityBadge from './PriorityBadge.jsx'; ในไฟล์ RequestCard.jsx|
|12:40| B4|• รับ prop priority• 'urgent' → แสดง “เร่งด่วน” className="priority-urgent"• 'normal' → แสดง “ปกติ” className="priority-normal"|ให้รองรับการแปลงค่า urgent เป็น "เร่งด่วน" และ normal เป็น "ปกติ" พร้อมกำหนด className ตามเงื่อนไขของโจทย์ได้เลยครับ:|เเก้ไฟล์PriorityBadge.jsx เเละไปเพิ่มcode เเละเเก้ ตัวนี้<span className={isUrgent ? 'priority-urgent' : 'priority-normal'}>{isUrgent ? 'เร่งด่วน' : 'ปกติ'}</span>|
|13:30| B4|นำ ไปใช้ใน RequestCardแทน {request.priority} ดิบ ด้วย (ดูTODO B4 ในไฟล์)| นำไปใช้ใน RequestCard ให้เรียบร้อยครับ โดยแทนที่ข้อความดิบเดิมด้วยคอมโพเนนต์ <PriorityBadge priority="{request.priority}"/> เพื่อให้ทุกการ์ดแสดงป้ายระดับความสำคัญตามสีและเงื่อนไขที่เราทำไว้| ตรวจสอบงานทั้งหมด|